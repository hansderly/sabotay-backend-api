import { register, login } from './auth';
import { createGroup, groupDetail, allGroup, deleteGroup } from './group';

export {
  // auth
  register,
  login,
  //   group
  createGroup,
  groupDetail,
  allGroup,
  deleteGroup,
};
